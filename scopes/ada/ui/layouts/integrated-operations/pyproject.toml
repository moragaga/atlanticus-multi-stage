[build-system]
requires = ["setuptools==83.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ada-ui-layout-integrated-operations"
version = "0.1.1"
description = "Layout semántico de Operaciones Integradas para ADA"
requires-python = "==3.14.2"
classifiers = [
    "Private :: Do Not Upload",
]
dependencies = [
    "ada-contracts-tool-manifest==0.1.0",
    "ada-ui-component-component-container==0.1.1",
    "atlanticus-web==0.1.0",
    "dash==4.4.1",
]

[tool.uv.sources]
ada-contracts-tool-manifest = { workspace = true }
ada-ui-component-component-container = { workspace = true }

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
namespaces = true
include = ["ada.ui.layouts.integrated_operations*"]

[tool.setuptools.package-data]
"ada.ui.layouts.integrated_operations" = [
    "py.typed",
    "resources/css/*.css",
    "resources/css/css.list",
]
