[build-system]
requires = ["setuptools==83.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "atlanticus-web-navigation"
version = "0.1.0"
description = "Contratos de navegación transversal para aplicaciones web Atlanticus"
requires-python = "==3.14.2"
classifiers = [
    "Private :: Do Not Upload",
]
dependencies = [
    "atlanticus-web==0.1.0",
    "atlanticus-web-users==0.1.0",
]

[tool.uv.sources]
atlanticus-web = { workspace = true }
atlanticus-web-users = { workspace = true }

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
namespaces = true
include = ["atlanticus.web.navigation*"]

[tool.setuptools.package-data]
"atlanticus.web.navigation" = ["py.typed"]
