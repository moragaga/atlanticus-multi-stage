[build-system]
requires = ["setuptools==83.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "atlanticus-web-users-local"
version = "0.1.0"
description = "Fuente local de usuarios para aplicaciones web Atlanticus"
requires-python = "==3.14.2"
classifiers = [
    "Private :: Do Not Upload",
]
dependencies = [
    "atlanticus-web-users==0.1.0",
]

[tool.uv.sources]
atlanticus-web-users = { workspace = true }

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
namespaces = true
include = ["atlanticus.web.users.local*"]

[tool.setuptools.package-data]
"atlanticus.web.users.local" = ["py.typed"]
