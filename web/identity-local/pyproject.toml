[build-system]
requires = ["setuptools==83.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "atlanticus-web-identity-local"
version = "0.1.0"
description = "Proveedor local de identidad para aplicaciones web Atlanticus"
requires-python = "==3.14.2"
classifiers = [
    "Private :: Do Not Upload",
]
dependencies = [
    "atlanticus-web-identity==0.1.0",
    "Flask==3.1.3",
]

[tool.uv.sources]
atlanticus-web-identity = { workspace = true }

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
namespaces = true
include = ["atlanticus.web.identity.local*"]

[tool.setuptools.package-data]
"atlanticus.web.identity.local" = ["py.typed"]
